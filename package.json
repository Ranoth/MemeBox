{
  "name": "MemeBox",
  "version": "1.5.0",
  "main": "index.js",
  "repository": "https://github.com/Ranoth/MemeBox.git",
  "author": "Ranoth <47944849+Ranoth@users.noreply.github.com>",
  "license": "MIT",
  "standard-version": {
    "scripts": {
      "postbump": "node update-version.js && git add app.xml MemeBox.csproj"
    }
  },
  "scripts": {
    "release": "standard-version --commitAll && git push --follow-tags origin master && gren release --override"
  },
  "devDependencies": {
    "standard-version": "^9.5.0",
    "xml2js": "^0.6.2"
  }
}
